rootProject.name = "karthick-vinod-service-server"

include("karthick-vinod-service-models", "karthick-vinod-service-server")

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}
